// Zustand store — placeholder for Step 1.
// Full implementation (streak, mastered tricks, dog profile, AsyncStorage persistence)
// will be added in a future step.
import { create } from 'zustand';

type AppState = {
  // placeholder — expanded in a future step
};

export const useAppStore = create<AppState>()(() => ({}));
