// Placeholder — animated clicker logic (Reanimated + expo-av) added in a future step.
import { Pressable, Text } from 'react-native';

export function ClickerButton() {
  return (
    <Pressable className="items-center justify-center w-48 h-48 rounded-full bg-cyan-500">
      <Text className="text-white text-2xl font-bold">CLICK</Text>
    </Pressable>
  );
}
